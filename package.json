{
  "name": "kayron",
  "version": "1.0.0",
  "description": "Kayron Calloway's portfolio website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx serve .",
    "build": "echo 'No build step required for vanilla HTML/JS'",
    "lint": "echo 'No linter configured'",
    "test": "echo 'No tests configured'"
  },
  "dependencies": {
    "@vercel/analytics": "^1.1.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
